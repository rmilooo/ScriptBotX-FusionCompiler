package org.Fusion.Lexer;

public enum TokenType {
    BOT,
    TOKEN,
    ON_READY,
    COMMAND,
    REPLY,
    USER,
    STRING,
    LOG,
    EOF,
    SET_COMMAND_PREFIX,
    WHITESPACE, BRACE, UNKNOWN
}
